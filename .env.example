# Anthropic Claude API key used by the AI agents.
#
# In the running app the key is entered on the Settings screen and stored in
# AsyncStorage (device-local). This file documents the variable name a team
# would wire up via react-native-config if they prefer build-time injection.
ANTHROPIC_API_KEY=your_key_here
